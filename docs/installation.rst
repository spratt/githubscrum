============
Installation
============

At the command line::

    $ easy_install githubscrum

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv githubscrum
    $ pip install githubscrum